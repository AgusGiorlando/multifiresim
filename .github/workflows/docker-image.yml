name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: 'Hello World'
        run: echo "Hello World"
